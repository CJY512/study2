management:
  endpoint:
    shutdown:
      enabled: true
    health:
#      show-details: always
      show-components: always
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
  server:
    port: 8080
  endpoints:
    web:
      exposure:
        include: "*"
info:
  app:
    name: hello-actuator
    company: cjy
logging:
  level:
    hello.controller: debug

server:
  tomcat:
    mbeanregistry:
      enabled: true
